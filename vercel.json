{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "export DIRECT_DATABASE_URL=\"${DIRECT_DATABASE_URL:-$(echo \"$DATABASE_URL\" | sed 's/-pooler//')}\" && prisma generate && prisma db push --skip-generate && (tsx prisma/seed.ts || echo 'seed step skipped') && next build",
  "framework": "nextjs"
}
